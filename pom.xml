<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.5</version>
		<relativePath/>
	</parent>

	<groupId>com.stucray</groupId>
	<artifactId>limen</artifactId>
	<version>0.3.1-SNAPSHOT</version>

	<properties>
		<java.version>26</java.version>
		<maven.compiler.source>26</maven.compiler.source>
		<maven.compiler.target>26</maven.compiler.target>
		<error-prone.version>2.49.0</error-prone.version>
		<nullaway.version>0.13.4</nullaway.version>
		<playwright.version>1.50.0</playwright.version>
		<maven-failsafe-plugin.version>3.5.4</maven-failsafe-plugin.version>
		<maven-jxr-plugin.version>3.6.0</maven-jxr-plugin.version>
		<maven-pmd-plugin.version>3.28.0</maven-pmd-plugin.version>
		<!--
			maven-pmd-plugin 3.28.0 bundles PMD 7.17, which doesn't support Java 26
			classfiles (major version 70). Override to 7.21.0+ via plugin dependencies.
		-->
		<pmd.version>7.24.0</pmd.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.modulith</groupId>
				<artifactId>spring-modulith-bom</artifactId>
				<version>2.0.6</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<!--
			OpenTelemetry: ships the OTel API, the Micrometer→OTel bridge, and
			OTLP exporters for metrics, traces, and logs. Auto-instruments
			Tomcat, RestTemplate, JDBC, etc. Pushes signals over OTLP/HTTP to
			the endpoint at OTEL_EXPORTER_OTLP_ENDPOINT (default
			http://localhost:4318); Spring Boot's Docker Compose support
			auto-detects the `grafana/otel-lgtm` service and wires the
			endpoint when the `observability` compose profile is up. Disabled
			under the `test` profile (see application-test.yaml).
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-opentelemetry</artifactId>
		</dependency>
		<!--
			Logback → OTel bridge. The Spring Boot OpenTelemetry starter
			configures an SdkLoggerProvider + OTLP log exporter, but does
			NOT ship a Logback appender to feed it. Without this dep,
			management.opentelemetry.logging.export.otlp.endpoint is set but
			zero log records reach Loki. The "1.0" in the artifact name is
			the Logback API line, not the artifact's own version; alpha is
			the only release stream for OTel instrumentation, so no GA tag
			exists despite the line being widely used in production.
		-->
		<dependency>
			<groupId>io.opentelemetry.instrumentation</groupId>
			<artifactId>opentelemetry-logback-appender-1.0</artifactId>
			<!--
				Version pinned to match the io.opentelemetry:* line that
				Spring Boot 4.0.5 ships (opentelemetry-bom 1.55.0). The
				instrumentation BOM 2.21.0-alpha → otel-bom 1.55.0; later
				instrumentation alphas track newer otel-bom versions and
				hit NoSuchMethodError on appender startup. Bump this in
				lockstep with Spring Boot's OpenTelemetry line.
			-->
			<version>2.21.0-alpha</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-docker-compose</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-authorization-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<!--
			In-memory token-bucket rate limiting for pre-auth endpoints
			(/oauth2/token, /signup, /forgot-password). Pure bucket4j-core only
			— not the spring-boot-starter (we wire the filter ourselves) and not
			bucket4j-postgresql (state lives in a per-process ConcurrentHashMap;
			a Postgres-backed swap is the v3.5 horizontal-scale story).
		-->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j-core</artifactId>
			<version>8.10.1</version>
		</dependency>

		<!--
			Distributed lock for @Scheduled methods. Adopted from day one so
			multi-instance deployments coordinate the signing-key rotation job
			(SigningKeyRotationSchedule) without retrofitting later. JDBC
			provider stores the `shedlock` table in the same Postgres as
			everything else (Flyway V12 creates it). 7.x is the Spring Boot 4
			line.
		-->
		<dependency>
			<groupId>net.javacrumbs.shedlock</groupId>
			<artifactId>shedlock-spring</artifactId>
			<version>7.7.0</version>
		</dependency>
		<dependency>
			<groupId>net.javacrumbs.shedlock</groupId>
			<artifactId>shedlock-provider-jdbc-template</artifactId>
			<version>7.7.0</version>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-authorization-server-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.microsoft.playwright</groupId>
			<artifactId>playwright</artifactId>
			<version>${playwright.version}</version>
			<scope>test</scope>
		</dependency>

		<!--
			Spring Modulith — application-modules verifier (slice 1) plus the
			JDBC publication registry that backs at-least-once delivery for
			`@ApplicationModuleListener`-annotated handlers (slice 2).
			`spring-modulith-api` is compile-scope so `@NamedInterface`
			annotations on `package-info.java` resolve in main sources;
			`spring-modulith-events-api` (compile) is needed for the
			`@ApplicationModuleListener` annotation on `AuditDispatcher`
			(2.0.x split it out of `spring-modulith-api`).
			`spring-modulith-starter-test` is test-scope and brings the
			`ApplicationModules` verifier API plus `Scenario`/`PublishedEvents`
			helpers. `spring-modulith-starter-jdbc` is runtime-scope and wires
			the JDBC `EventPublicationRegistry` against our existing
			`spring-boot-starter-data-jdbc` DataSource — Flyway V10 creates the
			`event_publication` table to the schema the registry expects.
		-->
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-events-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-starter-jdbc</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.modulith</groupId>
			<artifactId>spring-modulith-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<!--
						Auto-activate the `mailpit` Spring profile when running
						the app via `mvn spring-boot:run` so emails are
						captured by the Mailpit container declared in
						docker-compose.yml (web inbox at http://localhost:8025).
						Scoped to the run goal — `mvn test`/`verify` and the
						`build-image` goal are unaffected, so the test suite
						and production image keep the `logging` default.
					-->
					<profiles>
						<profile>mailpit</profile>
					</profiles>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.15.0</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<showWarnings>true</showWarnings>
					<failOnWarning>false</failOnWarning>
					<compilerArgs>
						<arg>-XDcompilePolicy=simple</arg>
						<arg>--should-stop=ifError=FLOW</arg>
						<arg>-XDaddTypeAnnotationsToSymbol=true</arg>
						<arg>-Xplugin:ErrorProne -XepDisableWarningsInGeneratedCode -Xep:NullAway:ERROR -XepOpt:NullAway:JSpecifyMode=true -XepOpt:NullAway:AnnotatedPackages=com.stucray.limen -XepOpt:NullAway:ExcludedFieldAnnotations=org.springframework.beans.factory.annotation.Autowired,org.mockito.Mock,org.mockito.InjectMocks</arg>
					</compilerArgs>
					<annotationProcessorPaths>
						<path>
							<groupId>com.google.errorprone</groupId>
							<artifactId>error_prone_core</artifactId>
							<version>${error-prone.version}</version>
						</path>
						<path>
							<groupId>com.uber.nullaway</groupId>
							<artifactId>nullaway</artifactId>
							<version>${nullaway.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
				<executions>
					<execution>
						<id>default-testCompile</id>
						<configuration>
							<!--
								NullAway off on test compile: tests construct fixtures with the
								`new Entity(null, ...)` Spring Data convention, wrap encoder
								calls casually, and dereference findById results in arrange-act-assert
								blocks where a null would already trip the assertion. Enforcing
								null-safety on test code adds boilerplate without reducing
								production-NPE risk. Error Prone still runs.
							-->
							<compilerArgs>
								<arg>-XDcompilePolicy=simple</arg>
								<arg>--should-stop=ifError=FLOW</arg>
								<arg>-XDaddTypeAnnotationsToSymbol=true</arg>
								<arg>-Xplugin:ErrorProne -XepDisableWarningsInGeneratedCode -Xep:NullAway:OFF -XepOpt:NullAway:AnnotatedPackages=com.stucray.limen</arg>
							</compilerArgs>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<environmentVariables>
						<LIMEN_SECURITY_KEK>dGVzdC1rZWstdGVzdC1rZWstdGVzdC1rZWstdGVzdC0=</LIMEN_SECURITY_KEK>
						<!--
							Universal kill switch for the OpenTelemetry SDK
							during tests. Without it, the SDK schedules
							periodic OTLP exports to localhost:4318 and floods
							test logs with connection-refused warnings (the
							LGTM container is dev-only, never up during
							`mvn verify`). Applies to surefire and failsafe.
						-->
						<OTEL_SDK_DISABLED>true</OTEL_SDK_DISABLED>
					</environmentVariables>
					<!--
						Default-off: every unit/integration test shares the
						127.0.0.1 client IP, so production-shaped buckets
						(capacity 10/hour for /signup) would 429-out other
						tests. RateLimitFilterIntegrationTest re-enables via
						@TestPropertySource with shrunken limits.
					-->
					<systemPropertyVariables>
						<limen.rate-limit.enabled>false</limen.rate-limit.enabled>
					</systemPropertyVariables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>${maven-failsafe-plugin.version}</version>
				<configuration>
					<includes>
						<include>**/*UiIT.java</include>
					</includes>
					<environmentVariables>
						<LIMEN_SECURITY_KEK>dGVzdC1rZWstdGVzdC1rZWstdGVzdC1rZWstdGVzdC0=</LIMEN_SECURITY_KEK>
						<!--
							Universal kill switch for the OpenTelemetry SDK
							during tests. Without it, the SDK schedules
							periodic OTLP exports to localhost:4318 and floods
							test logs with connection-refused warnings (the
							LGTM container is dev-only, never up during
							`mvn verify`). Applies to surefire and failsafe.
						-->
						<OTEL_SDK_DISABLED>true</OTEL_SDK_DISABLED>
					</environmentVariables>
					<!-- Same rationale as surefire above. -->
					<systemPropertyVariables>
						<limen.rate-limit.enabled>false</limen.rate-limit.enabled>
					</systemPropertyVariables>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.14</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jxr-plugin</artifactId>
				<version>${maven-jxr-plugin.version}</version>
				<executions>
					<execution>
						<id>jxr-source-xref</id>
						<phase>verify</phase>
						<goals>
							<goal>jxr</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
				<version>${maven-pmd-plugin.version}</version>
				<configuration>
					<rulesets>
						<ruleset>${project.basedir}/pmd-ruleset.xml</ruleset>
					</rulesets>
					<!--
						XML output feeds scripts/render-pmd-report.py, which
						regenerates docs/reports/code-quality.{md,json} during
						the verify phase. For an interactive HTML drilldown,
						run `mvn pmd:pmd -Dformat=html` ad-hoc.
					-->
					<format>xml</format>
					<linkXRef>true</linkXRef>
					<printFailingErrors>true</printFailingErrors>
					<skipPmdError>true</skipPmdError>
					<includeTests>false</includeTests>
				</configuration>
				<executions>
					<execution>
						<id>pmd-report</id>
						<phase>verify</phase>
						<goals>
							<goal>pmd</goal>
						</goals>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>net.sourceforge.pmd</groupId>
						<artifactId>pmd-core</artifactId>
						<version>${pmd.version}</version>
					</dependency>
					<dependency>
						<groupId>net.sourceforge.pmd</groupId>
						<artifactId>pmd-java</artifactId>
						<version>${pmd.version}</version>
					</dependency>
					<dependency>
						<groupId>net.sourceforge.pmd</groupId>
						<artifactId>pmd-javascript</artifactId>
						<version>${pmd.version}</version>
					</dependency>
					<dependency>
						<groupId>net.sourceforge.pmd</groupId>
						<artifactId>pmd-jsp</artifactId>
						<version>${pmd.version}</version>
					</dependency>
				</dependencies>
			</plugin>
			<!--
				Run scripts/render-pmd-report.py after maven-pmd-plugin in
				the verify phase. Plugins within a phase run in declaration
				order, so this stays after the pmd-report execution above
				and reads the freshly-written target/pmd.xml. The script is
				stdlib-only Python and writes docs/reports/code-quality.{md,json}
				atomically. See docs/process/code-quality.md.
			-->
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>render-pmd-report</id>
						<phase>verify</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>python3</executable>
							<workingDirectory>${project.basedir}</workingDirectory>
							<arguments>
								<argument>scripts/render-pmd-report.py</argument>
								<argument>--in</argument>
								<argument>target/pmd.xml</argument>
								<argument>--out-md</argument>
								<argument>docs/reports/code-quality.md</argument>
								<argument>--out-json</argument>
								<argument>docs/reports/code-quality.json</argument>
								<argument>--ruleset</argument>
								<argument>pmd-ruleset.xml</argument>
							</arguments>
						</configuration>
					</execution>
					<!--
						Run scripts/coverage-report.sh after JaCoCo's report
						execution (bound to the test phase) has written
						target/site/jacoco/jacoco.csv. The script splices the
						auto-block between the markers in test-coverage.md and
						appends a snapshot to test-coverage-history.jsonl. The
						"Re-running this report" section of test-coverage.md
						documents the flow.
					-->
					<execution>
						<id>render-coverage-report</id>
						<phase>verify</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>bash</executable>
							<workingDirectory>${project.basedir}</workingDirectory>
							<arguments>
								<argument>scripts/coverage-report.sh</argument>
								<argument>--write</argument>
								<argument>docs/reports/test-coverage.md</argument>
							</arguments>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>

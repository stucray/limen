spring:
  application:
    name: auth-server
  datasource:
    url: jdbc:postgresql://localhost:5433/auth
    username: auth
    password: secret
  flyway:
    enabled: true
server:
  port: 8090
  servlet:
    session:
      timeout: 30m
management:
  endpoints:
    web:
      exposure:
        include: health
